# Release Acceptance: v1.0.0

This document records the accepted release evidence for HarborShield `v1.0.0`.

For `v1.0.0`, the release decision reuses the fully executed published-bundle evidence captured on the `v0.1.0-rc4` release-candidate line, plus the signed `v1.0` scope, S3 contract, and operator-manageability documents added afterward.

## Acceptance Record

| Field | Value |
| --- | --- |
| Run date | 2026-03-24 |
| Operator | Codex local acceptance pass |
| Release decision basis | `v0.1.0-rc4` published-bundle acceptance evidence plus signed `v1.0` release-decision docs |
| Outcome | pass |

## Evidence Basis

The `v1.0.0` decision is based on:

- published-bundle install evidence
- published-bundle first-run evidence
- published-bundle core S3 evidence
- published-bundle backup evidence
- published-bundle restore evidence
- published-bundle distributed beta workflow evidence
- signed `v1.0` scope note
- signed `v1.0` S3 contract
- signed `v1.0` operator-manageability sweep

Primary references:

- [`./v0.1.0-rc4.md`](./v0.1.0-rc4.md)
- [`../v1-scope.md`](../v1-scope.md)
- [`../v1-s3-contract.md`](../v1-s3-contract.md)
- [`../v1-operator-sweep.md`](../v1-operator-sweep.md)

## Release Decision

This is accepted as sufficient for `v1.0.0` because:

- the runtime/product evidence was already captured from the published release bundle
- the subsequent changes were release-decision documents, not product/runtime code changes
- the blocker register now records no remaining open `critical` or `high` `v1.0` blockers

## Scope Reminder

`v1.0.0` GA applies to:

- `single-node`

`distributed` remains:

- beta

## Exit Rule

This acceptance record is sufficient for `v1.0.0` if the tagged release workflow passes on the `v1.0.0` tag and publishes the final stable assets successfully.
